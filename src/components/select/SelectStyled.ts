import styled from "styled-components";

const Styled = {
  Icon: styled("img")({
    backgroundSize: "20px",
    width: "20px",
    height: "20px",
    marginRight: "20px",
    transform: "translateY(4px)",
  }),
  Select: {
    width: "200px",
    borderRadius: "15px",
    height: "50px",
  },
};

export default Styled;
